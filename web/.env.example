# Backend base URL used by the frontend.
# Local dev: leave blank (defaults to http://localhost:8080).
# Production (Vercel): set to your Render backend URL, e.g. https://sivp-backend.onrender.com
VITE_API_URL=
